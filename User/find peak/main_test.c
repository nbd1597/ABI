#include <stdio.h>
#include "find_peak_nibp.h"
#include "pulse_data.h"



//int main()
//{
    //find_peak((float*)pulse_data1);
    //find_MAP((float*)pulse_data1);
    /*printf("\n-part1-\n");
    FILE *fp1;
    fp1 = fopen ("no_DC_output.txt", "w");
    char buf[32];
    remove_DC(pulse_data1, 0.9999);

    for (int i = 0; i < 2500; i++)
    {
        sprintf(buf,"%d\n", pulse_no_DC[i]);
        fputs(buf, fp1);              
    }
    fclose(fp1);*/
   // return 0;
//}

